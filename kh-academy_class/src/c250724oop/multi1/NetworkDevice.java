package c250724oop.multi1;

// 다중상속 전용 클래스
public interface NetworkDevice {
	// 필드 : 생성 불가
	// 상수는 가능 public static final int a = 10;
	// 메소드는 추상 메소드만 가능하다
	// 생성자도 만들 수 없다 (public abstract 생략)
	// super는 쓸 수 없다
}