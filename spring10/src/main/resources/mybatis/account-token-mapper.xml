<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="accountToken">
	<insert id="insert">
		insert into account_token(account_token_no, account_token_target, account_token_value)
		values (#{accountTokenNo}, #{accountTokenTarget}, #{accountTokenValue})
	</insert>
</mapper>