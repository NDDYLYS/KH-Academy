<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="book">

	<select id="sequence" resultType="int">
		select book_seq.nextval from dual
	</select>
	
	<insert id="insert">
	  	insert into book(book_id, book_title, book_price, book_page_count, book_genre 
	  	book_author, book_publisher, book_publication_date) values 
	  	(#{bookId}, #{bookTitle}, #{bookPrice}, #{bookPageCount}, #{bookGenre}, 
	  	#{bookAuthor}, #{bookPublisher}, #{bookPublicationDate})
  	</insert>
  
</mapper>