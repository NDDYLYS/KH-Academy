<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pokemon">

<!-- 등록 -->
	<select id="sequence" resultType="int">
		select pokemon_seq.nextval from dual
	</select>
	
	<insert id="insert">
	  	insert into pokemon(pokemon_no, pokemon_name, 
	  	pokemon_type, pokemon_like) values (#{pokemonNo}, 
	  	#{pokemonName}, #{pokemonType}, 0)
  	</insert>
<!-- 등록 -->

<!-- 목록 -->
	<select id="list" resultType="PokemonDto">
		select * from pokemon order by pokemon_no asc
	</select>
<!-- 목록 -->

<!-- 상세조회 -->
	<select id="detail" resultType="PokemonDto">
		select * from pokemon where pokemon_no = #{pokemonNo}
	</select>
<!-- 상세조회 -->
  
<!--   삭제 -->
	<delete id="delete">
		delete pokemon where pokemon_no = #{pokemonNo}
	</delete>
<!--   삭제 -->

<!-- 수정 -->
	<update id="update">
		update pokemon set
		pokemon_name=#{pokemonName}, pokemon_type=#{pokemonType}, pokemon_like=#{pokemonLike}
		where pokemon_no=#{pokemonNo}
	</update>
<!-- 수정 -->
  
</mapper>